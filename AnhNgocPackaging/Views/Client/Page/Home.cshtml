@model ListComponentResultDto;
@{
    var list = Model.Items?.OrderBy(x => x.Priorty).ToList() ;
}

@if (@list != null && @list.Count() > 0)
{
    @foreach (var item in list)
    {
        @await Component.InvokeAsync(item.ComponentName, item)
    }
}
